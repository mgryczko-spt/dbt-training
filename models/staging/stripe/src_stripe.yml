version: 2

sources:
  - name: stripe
    description: Staged source from Stripe payment data
    database: raw
    freshness:
      error_after:
        count: 2
        period: day
      warn_after:
        count: 1
        period: day
    loaded_at_field: _batched_at
    tables:
      - name: payment
        description: Staged payment view from strip_payment source
